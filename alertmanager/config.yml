global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'instance']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email-me'
  routes:
    - match:
        severity: critical
      receiver: 'email-me'
      continue: true

receivers:
  - name: 'email-me'
    email_configs:
      - to: 'vasilysa.lebedeva@gmail.com'
        from: 'vasilysa.lebedeva@gmail.com'
        smarthost: 'smtp.gmail.com:587'
        auth_username: 'vasilysa.lebedeva@gmail.com'
        auth_password: '.... enter yours'   
        send_resolved: true
        headers:
          Subject: '[{{ .Status | toUpper }}] {{ .GroupLabels.alertname }}'

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
